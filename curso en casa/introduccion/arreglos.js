const arreglo = ['texto', 456.10, false, {propiedad: 'valor'},[1,2, 3]] ;
console.log(arreglo);

const amigos = ['kevin', 'hader','carlos']
console.log(amigos[0])//el numero dentro del bracket es el indice de donde quiere coger//

const colores = [];
colores[0] = 'rojo';
colores[1] = 'verde';
colores[2] = 'blanco';
colores[3] = 'amarillo';

console.log('El usuario tiene ' + colores.length + ' colores');

colores.push('Azul');
colores.push('negro');//la propiedad push sube otro elemento a la variable y se agrega al final

console.log(colores);
console.log(colores.length)


